{
  "name": "rosies-arduino-test",
  "version": "1.0.0",
  "scripts": {
    "build": "clear && pio run -t upload",
    "monitor": "clear && pio device monitor",
    "start": "yarn build && yarn monitor"
  },
  "license": "MIT"
}
