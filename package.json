{
  "name": "rosies-arduino-test",
  "version": "1.0.0",
  "scripts": {
    "build": "pio run -t upload",
    "monitor": "pio device monitor --filter time",
    "start": "yarn build && yarn monitor"
  },
  "license": "GPL-3.0"
}
